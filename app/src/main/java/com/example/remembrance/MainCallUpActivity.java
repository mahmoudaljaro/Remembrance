package com.example.remembrance;

import androidx.appcompat.app.AppCompatActivity;
import androidx.fragment.app.Fragment;
import androidx.fragment.app.FragmentManager;
import androidx.fragment.app.FragmentTransaction;

import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.LinearLayout;

import com.google.android.material.bottomnavigation.BottomNavigationView;

public class MainCallUpActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);








       // setContentView(R.layout.activity_main_call_up);
       // MainInterfaceActivity mainInterfaceActivity=new MainInterfaceActivity();
        //MainSettingsActivity mainSettingsActivity=new MainSettingsActivity();

        //BottomNavigationView bottomNavigation=findViewById(R.id.bottomNavigation);
       // bottomNavigation.setOnItemSelectedListener(item -> {
          //  switch (item.getItemId()){
             //   case R.id.settings:

              //      replaceFregment(new MainSettingsActivity());
                 //   break;
              //  case R.id.home:
                    //replaceFregment(new MainInterfaceActivity());
                   // break;
            //}



           // return true;
        //});
    }
   // private void replaceFregment(Fragment fragment){

       // FragmentManager fragmentManager=getSupportFragmentManager();
      //  FragmentTransaction fragmentTransaction=fragmentManager.beginTransaction();
      //  fragmentTransaction.replace(R.id.FrameLayout,fragment);
       // fragmentTransaction.commit();



    //}
}